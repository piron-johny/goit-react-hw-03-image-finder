import { StyledHeader } from './SearchBar.styled';
import Form from '../Form';

const SearchBar = () => {
  return (
    <StyledHeader>
      <Form />
    </StyledHeader>
  );
};

export default SearchBar;
